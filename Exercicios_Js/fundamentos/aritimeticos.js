const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
//operadores aritiméticos do javascritps funcionam sempre com operações com dois operandos sempre, operações binárias
//diferente das operações unárias que envolvem apenas um operando. Exemplo: -d (negação)
//sintaxe "in-fix" 

//d++
//Exemplo de operador post-fix:a operador que vem depois do operando

//++d
//Exemplo de operador pre-fix: operador que vem antes do operando

//-d
//

const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a 
const modulo = a % 2
//Trás o resto de uma divisão 

console.log(soma, subtracao, multiplicacao,  divisao, modulo)